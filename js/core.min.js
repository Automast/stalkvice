(function () {
    // Dev/Preview mode
    var isDev = window.location.hostname === 'localhost' ||
        window.location.hostname === '127.0.0.1' ||
        window.location.hostname.includes('192.168.') ||
        window.location.protocol === 'file:' ||
        window.location.hostname.includes('vercel.app');

    // Funções globais
    window._0xGetCheckout = function () {
        return window.CONFIG.checkout.official;
    };

    window._0xGetApi = function () {
        return window.CONFIG.api.legacy;
    };

    if (isDev) {
        console.log('🔧 Dev/Preview mode');
    }

    // Update checkout links
    document.addEventListener('DOMContentLoaded', function () {
        document.querySelectorAll('a.pricing-cta-button, .pricing-cta-button').forEach(function (el) {
            if (el.tagName === 'A') {
                el.href = window.CONFIG.checkout.official;
            }
        });
    });
})();





